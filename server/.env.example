# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=medreminder

# Database Connection Retry Configuration
DB_MAX_RETRIES=5
DB_INITIAL_RETRY_DELAY=1000

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRES_IN=7d

# CORS Configuration
# Note: The server allows both localhost:5173 and localhost:8080 by default
# You can set a custom origin here if needed
CORS_ORIGIN=http://localhost:5173
